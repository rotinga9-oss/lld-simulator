# Get your FREE key from https://console.groq.com → API Keys → Create key (no card needed)
# Add this as GROQ_API_KEY in your Vercel project settings (Environment Variables)
GROQ_API_KEY=your_groq_api_key_here
